import torch
import torch.nn as nn

class YourModelName(nn.Module):
    def __init__(self):
        super(YourModelName, self).__init__()
        #define your layers and activation function
    
    def forward(self, x):
        #pass the input "x" through the layers we initialized in the init
        pass

